use_default_settings: true

server:
  secret_key: "observatory-dev-secret-change-in-production"
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false

redis:
  url: redis://valkey:6379/0

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json
