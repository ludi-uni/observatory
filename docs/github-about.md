# GitHub About

Suggested description:

> Observation layer for LLM agents. Search, fetch, extract, summarize, and store evidence-backed observations via MCP.

Suggested topics:

`mcp`, `llm`, `agent`, `ai-agent`, `observability`, `observation`, `search`, `searxng`, `playwright`, `trafilatura`, `rag`, `typescript`, `postgresql`
